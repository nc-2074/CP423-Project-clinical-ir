# requirements.txt
# ─────────────────
# Direct imports only — transitive deps are handled by install.sh
# DO NOT run: pip install -r requirements.txt  (resolver will choke)
# INSTEAD run: ./install.sh

# Core pipeline
pyannote.audio>=4.0.4
groq>=0.9.0
mlx-lm>=0.22.0

# LiveKit (compatible versions)
livekit>=1.0.0
livekit-api>=1.0.0
livekit-agents>=1.5.0
livekit-plugins-silero>=0.7.0
livekit-plugins-groq>=1.0.0

# IR / embeddings
sentence-transformers>=2.7.0
supabase>=2.0.0

# Web / API
flask>=3.0.0
flask-cors>=4.0.0
requests>=2.31.0

# Utilities
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
python-dotenv>=1.0.0
pydub>=0.25.1
huggingface_hub>=0.28.1
transformers>=4.48.0
tokenizers>=0.21
